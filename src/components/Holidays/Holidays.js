import { Typography } from "@mui/material";
import React from "react";

export default function Holiday(){

    return (<div className="container" style={{marginTop:140}}>
        <Typography variant="h3">Holiday List</Typography>

        </div>)
}